<%= form_for @todo do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.submit 'Create Todo' %>
<% end %>

<ul class='todos'>
  <% @todos.each do |todo| %>
    <li><%= todo.title %></li>
  <% end %>
</ul>
