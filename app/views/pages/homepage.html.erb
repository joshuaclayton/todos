<p data-role='welcome'>Welcome to the todos app!</p>

<%= form_for :session, url: session_path do |form| %>
  <%= form.label :email %>
  <%= form.text_field :email %>
  <%= form.submit 'Sign In' %>
<% end %>
